
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>รายการรอชำระเงิน | Dentaist Clinic System</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/styles.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1><i class="fas fa-file-invoice-dollar"></i> รายการรอชำระเงิน</h1>
        </div>

        <% if (visits.length > 0) { %>
            <table class="table table-hover table-bordered">
                <thead class="thead-light">
                    <tr>
                        <th>วันที่รับการรักษา</th>
                        <th>CN</th>
                        <th>ชื่อ - นามสกุล</th>
                        <th>จำนวนเงิน (บาท)</th>
                        <th class="text-center">ดำเนินการ</th>
                    </tr>
                </thead>
                <tbody>
                    <% visits.forEach(visit => { %>
                        <tr>
                            <td><%= new Date(visit.visit_date).toLocaleDateString('th-TH', { year: 'numeric', month: 'long', day: 'numeric' }) %></td>
                            <td><%= visit.clinic_number %></td>
                            <td><%= visit.first_name %> <%= visit.last_name %></td>
                            <td class="text-right"><%= visit.total_amount.toLocaleString('th-TH', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></td>
                            <td class="text-center">
                                <a href="/payment?visit_id=<%= visit.visit_id %>" class="btn btn-sm btn-success">
                                    <i class="fas fa-credit-card"></i> ชำระเงิน
                                </a>
                                <a href="/patients/<%= visit.patient_id %>/history" class="btn btn-sm btn-info">
                                    <i class="fas fa-history"></i> ดูประวัติ
                                </a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <div class="alert alert-info text-center">
                <i class="fas fa-check-circle"></i> ไม่มีรายการรอชำระเงิน
            </div>
        <% } %>

    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
