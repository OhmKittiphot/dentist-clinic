<!DOCTYPE html>
<html lang="th">

<head>
  <%- include('../partials/head') %>
  <!-- ✅ แก้ path ไม่ต้องมี /public -->
  <link rel="stylesheet" href="/public/css/staff-payments.css" />
  <title>รายการการชำระเงิน</title>
</head>

<body>
  <div class="app">
    <%- include('../partials/navbar') %>
    <main class="main-content">
      <div class="container">
        <h1 class="mb-4">รายการชำระเงินทั้งหมด</h1>

        <div class="payments-table">
          <div class="payments-thead">
            <div>ID</div>
            <div>ผู้ป่วย</div>
            <div>ยอดชำระ (฿)</div>
            <div>วัน-เวลาที่ชำระ</div>
            <div>สถานะ</div>
            <div>การดำเนินการ</div>
          </div>

          <% if (payments.length === 0) { %>
            <div class="empty-payments">ไม่มีข้อมูลการชำระเงิน</div>
          <% } else { %>
            <% payments.forEach(p => { %>
              <div class="payments-trow">
                <div><%= p.id %></div>
                <div><%= p.patient_name || '-' %></div>
                <div><%= p.amount ? p.amount.toFixed(2) : '-' %></div>
                <div>
                  <%= p.payment_date
                      ? new Date(p.payment_date).toLocaleString('th-TH', { timeZone: 'Asia/Bangkok' })
                      : '-' %>
                </div>
                <div>
                  <% if (p.status === 'paid') { %>
                    <span class="status-badge status-paid">ชำระแล้ว</span>
                  <% } else { %>
                    <span class="status-badge status-pending">รอชำระ</span>
                  <% } %>
                </div>
                <div>
                  <% if (p.status === 'pending') { %>
                    <form action="/staff/payments/<%= p.id %>/complete" method="POST" style="display:inline;">
                      <button type="submit" class="btn success">
                        ยืนยันการชำระ
                      </button>
                    </form>
                  <% } else { %>
                    <span>-</span>
                  <% } %>
                </div>
              </div>
            <% }) %>
          <% } %>
        </div>
      </div>
    </main>
  </div>

  <!-- ✅ ปลอดภัยตาม CSP -->
  <script nonce="<%= nonce %>"></script>
</body>

</html>
