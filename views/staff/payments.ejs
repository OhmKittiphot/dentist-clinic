<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>รายการการชำระเงิน</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body class="bg-light">
    <div class="container my-4">
        <h2 class="mb-4">รายการการชำระเงินทั้งหมด</h2>

        <table class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>ID</th>
                    <th>ผู้ป่วย</th>
                    <th>Visit ID</th>
                    <th>Staff ID</th>
                    <th>ยอดชำระ (฿)</th>
                    <th>วันชำระ</th>
                    <th>สถานะ</th>
                    <th>การดำเนินการ</th>
                </tr>
            </thead>
            <tbody>
                <% if (payments.length === 0) { %>
                    <tr><td colspan="8" class="text-center text-muted">ไม่มีข้อมูลการชำระเงิน</td></tr>
                <% } else { %>
                    <% payments.forEach(p => { %>
                        <tr>
                            <td><%= p.id %></td>
                            <td><%= p.patient_name || '-' %></td>
                            <td><%= p.visit_id %></td>
                            <td><%= p.staff_id %></td>
                            <td><%= p.amount.toFixed(2) %></td>
                            <td><%= p.payment_date %></td>
                            <td>
                                <% if (p.status === 'paid') { %>
                                    <span class="badge badge-success">ชำระแล้ว</span>
                                <% } else { %>
                                    <span class="badge badge-warning">รอชำระ</span>
                                <% } %>
                            </td>
                            <td>
                                <% if (p.status === 'pending') { %>
                                    <form action="/staff/payments/<%= p.id %>/complete" method="POST" style="display:inline;">
                                        <button type="submit" class="btn btn-sm btn-success">
                                            <i class="fas fa-check"></i> ชำระแล้ว
                                        </button>
                                    </form>
                                <% } else { %>
                                    <span class="text-muted">-</span>
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                <% } %>
            </tbody>
        </table>
    </div>
    <script src="https://kit.fontawesome.com/a2e0f5a57d.js" crossorigin="anonymous"></script>
</body>
</html>
